---
phase: 02-effects-system-redesign
plan: 05
type: execute
wave: 4
depends_on:
  - 02-02
  - 02-03
  - 02-04
files_modified: []
autonomous: false
requirements:
  - FX-02
user_setup: []

must_haves:
  truths:
    - "Particle effects during peak merge activity maintain 50-60fps in a browser/WASM build"
    - "FPS counter shows stable 50+ fps during multi-box merge with max particles active"
    - "No visible frame drops or stuttering during chest open, fly-to-bar, or celebration effects on web"
  artifacts: []
  key_links: []
---

<objective>
Build the web version and verify that all new effects maintain 50-60fps on browser/WASM. This is the performance gate for the entire phase -- if web performance is below target, identify bottlenecks and tune tier budgets before the phase is complete.

Purpose: FX-02 requires verified web performance. Without this checkpoint, the phase cannot be marked complete. The MED tier particle budgets may need adjustment based on actual browser performance.
Output: Verified web build with FPS measurements during peak effect activity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-effects-system-redesign/02-CONTEXT.md
@.planning/phases/02-effects-system-redesign/02-RESEARCH.md
@.planning/phases/02-effects-system-redesign/02-01-SUMMARY.md
@.planning/phases/02-effects-system-redesign/02-02-SUMMARY.md
@.planning/phases/02-effects-system-redesign/02-03-SUMMARY.md
@.planning/phases/02-effects-system-redesign/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build web version and run FPS baseline</name>
  <files></files>
  <read_first>love-web-builder-main/README.md, main.lua, mobile.lua</read_first>
  <action>
Build the web version using love-web-builder and prepare it for browser testing.

1. Read the love-web-builder README to confirm the build command.
2. Run the web build:
```bash
cd /home/username/Documents/Coins-Boxes/love-web-builder-main && ./build.sh /home/username/Documents/Coins-Boxes/
```
3. Verify the build succeeds (output files exist).
4. Verify that the game already has an FPS counter visible (main.lua line ~166). If it exists, note the location. If not, add a simple FPS display:
```lua
-- In love.draw(), after all screen drawing:
love.graphics.setColor(1, 1, 0, 0.8)
love.graphics.print("FPS: " .. love.timer.getFPS(), 10, 10)
```
5. Report the build output directory and how to open it in browser (typically `open index.html` from the output directory, or serve via `python3 -m http.server`).
  </action>
  <verify>
    <automated>cd /home/username/Documents/Coins-Boxes/love-web-builder-main && ls -la build/ 2>/dev/null || echo "Build directory check" && grep -c "getFPS" /home/username/Documents/Coins-Boxes/main.lua</automated>
  </verify>
  <acceptance_criteria>
    - Web build completes without errors
    - Build output directory contains HTML/JS/WASM files
    - FPS counter is visible in the game (either existing or added)
    - Instructions for browser testing are documented in the summary
  </acceptance_criteria>
  <done>Web build is ready for browser testing with visible FPS counter.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify web performance at 50-60fps during peak effects</name>
  <files></files>
  <action>
Human verification checkpoint. Present the web build to the user for browser testing with the verification steps below. No automated action needed -- the web build was prepared in Task 1.
  </action>
  <what-built>
Complete effects system redesign: tiered particle system (HIGH/MED/LOW), merge celebrations (CS chunky fragments + Arena glow/dissolve), coin fly-up on merge, chest open effects, fly-to-bar icons, big reward flash, button bounce, tab slide, generator pulse, and order glow. Web build compiled with all effects active.
  </what-built>
  <how-to-verify>
1. Open the web build in a browser (Chrome recommended):
   - Either serve from the build directory: `cd love-web-builder-main/build && python3 -m http.server 8080` then open `http://localhost:8080`
   - Or open the index.html directly

2. **CS merge performance test:**
   - Play Coin Sort, deal coins, merge multiple boxes
   - Watch FPS counter during multi-box merge animation (peak particle activity)
   - Verify coin fly-up arc on merge
   - Verify fuel/star icons fly to resource bar on merge
   - Expected: FPS stays at 50-60fps

3. **Arena effects test:**
   - Switch to Arena, merge items (verify dissolve effect, not fragments)
   - Tap a chest (verify shake + particles)
   - Complete an order (verify star fly-to-bar icons)
   - If possible, complete a level (verify big reward flash)

4. **UI polish test:**
   - Switch tabs -- highlight bar should slide smoothly
   - Press buttons -- should shrink to 95% and bounce back
   - Observe generators in Arena -- should pulse gently
   - Check completable orders -- should have green glow

5. **Performance verdict:**
   - FPS counter shows 50+ during all effects: PASS
   - FPS drops below 50 during peak effects: NOTE which effect and by how much

Report: "approved" if FPS >= 50 during peak effects, or describe specific issues.
  </how-to-verify>
  <verify>
    <automated>echo "Human verification checkpoint -- no automated test"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms FPS >= 50 during peak effects in web browser
    - User confirms all visual effects render correctly (merge celebrations, coin fly-up, chest open, fly-to-bar, flash, tab slide, button bounce, generator pulse, order glow)
    - User types "approved" or provides specific issues to address
  </acceptance_criteria>
  <done>User has verified web performance meets 50-60fps target during peak effects and all visual effects render correctly in browser.</done>
  <resume-signal>Type "approved" if web FPS >= 50 during peak effects. Otherwise describe which effects cause drops and to what FPS level.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Not applicable.

## STRIDE Threat Register

No threats identified.
</threat_model>

<verification>
1. Web build compiles successfully
2. Game loads in browser without JavaScript errors
3. FPS counter visible and reads 50-60 during normal gameplay
4. FPS counter maintains 50+ during peak effect activity (multi-merge + particles + flash)
5. All visual effects render correctly in web build (no missing effects, no visual glitches)
</verification>

<success_criteria>
- Web build loads and runs in browser
- FPS stays at 50-60fps during peak particle/effect activity
- All 5 FX requirements verified as working in web build
- Human confirms visual quality and performance are acceptable
</success_criteria>

<output>
After completion, create `.planning/phases/02-effects-system-redesign/02-05-SUMMARY.md`
</output>
